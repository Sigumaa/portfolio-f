name: portfolio
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0-375.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.10
  flutter_svg: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/
